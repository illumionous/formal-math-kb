[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "formal-math-kb"
version = "0.1.0"
description = "A Lean-verified knowledge base and SFT data pipeline for high-school mathematics"
requires-python = ">=3.10"
dependencies = []

[project.optional-dependencies]
dev = ["jsonschema>=4.20"]

[tool.setuptools.packages.find]
include = ["pipeline*"]
